<Page x:Class="JurassicRisk.Views.Connexion"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:JurassicRisk.Views"
      xmlns:Ressources="clr-namespace:JurassicRisk.Ressource"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="Connexion">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="20" FontSize="20" Text="{x:Static Ressources:Strings.Pseudo}"/>
        <TextBox Grid.Row="1" VerticalAlignment="Center" Margin="10" Style="{StaticResource ResourceKey=TextBoxBaseStyle}"></TextBox>

        <TextBox Grid.Row="2" VerticalAlignment="Center" Margin="10" Style="{StaticResource ResourceKey=TextBoxBaseStyle}"></TextBox>
    </Grid>
</Page>
